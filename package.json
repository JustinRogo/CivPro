{
  "name": "federal-civil-rules", "version": "0.1.0", "private": true,
  "type": "module", "engines": {"node": ">=24 <25"},
  "scripts": {
    "acquire": "python scripts/acquire.py",
    "ingest": "python scripts/ingest.py",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "node --test --test-isolation=none tests/*.test.mjs",
    "test:browser": "playwright test",
    "diff": "node scripts/diff.mjs",
    "check": "npm run validate && npm test && npm run build"
  },
  "devDependencies": {"@playwright/test": "1.61.1", "@axe-core/playwright": "4.12.1", "ajv": "8.17.1"}
}
